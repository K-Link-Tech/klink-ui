import { createStyles } from '@k-link/styles';

export default createStyles({
  root: {
    position: 'relative',
  },

  label: {
    position: 'absolute',
    top: '50%',
    transform: 'translateY(-50%)',
  },
});
