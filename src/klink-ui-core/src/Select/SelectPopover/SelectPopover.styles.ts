import { createStyles, rem } from '@klink-ui/styles';

export default createStyles(() => ({
  dropdown: {},

  itemsWrapper: {
    padding: rem(4),
    display: 'flex',
    width: '100%',
    boxSizing: 'border-box',
  },
}));
