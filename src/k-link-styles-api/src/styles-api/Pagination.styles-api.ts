import type { PaginationStylesNames } from '@k-link/core';

export const Pagination: Record<PaginationStylesNames, string> = {
  control: 'Control element: items, next/previous, first/last buttons',
  dots: 'Dots icon wrapper',
};
