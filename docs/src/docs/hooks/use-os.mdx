---
group: 'k-link-hooks'
package: '@k-link/hooks'
category: 'utils'
title: 'use-os'
order: 1
slug: /hooks/use-os/
description: 'Get user os'
import: "import { useOs } from '@k-link/hooks';"
docs: 'hooks/use-os.mdx'
source: 'k-link-hooks/src/use-os/use-os.ts'
---

import { HooksDemos } from '@k-link/demos';

## Usage

use-os detects user's os. Possible values are: `undetermined`, `macos`, `ios`, `windows`, `android`, `linux`.
If os cannot be identified, for example, during server side rendering `undetermined` will be returned.

<Demo data={HooksDemos.useOsDemo} />

## TypeScript

You can import `OS` union type from `@k-link/hooks`:

```tsx
import type { OS } from '@k-link/hooks';
// OS type is 'undetermined' | 'macos' | 'ios' | 'windows' | 'android' | 'linux'
```

## Definition

```tsx
function getOS(options?: {
  getValueInEffect: boolean;
}): 'undetermined' | 'macos' | 'ios' | 'windows' | 'android' | 'linux';
```
