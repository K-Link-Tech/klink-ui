---
group: 'k-link-hooks'
package: '@k-link/hooks'
category: 'state'
title: 'use-counter'
order: 1
slug: /hooks/use-counter/
description: 'Increment/decrement state within given boundaries'
import: "import { useCounter } from '@k-link/hooks';"
docs: 'hooks/use-counter.mdx'
source: 'k-link-hooks/src/use-counter/use-counter.ts'
---

import { HooksDemos } from '@k-link/demos';

## Usage

<Demo data={HooksDemos.useCounterDemo} />

## Definition

```tsx
function useCounter(
  initialValue?: number,
  options?: Partial<{
    min: number;
    max: number;
  }>
): [
  number,
  {
    increment: () => void;
    decrement: () => void;
    set: (value: number) => void;
    reset: () => void;
  }
];
```
