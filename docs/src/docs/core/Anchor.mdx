---
group: 'mantine-core'
package: '@klink-ui/core'
title: Anchor
order: 1
polymorphic: true
slug: /core/anchor/
category: 'navigation'
description: 'Display links with theme styles'
props: ['Anchor']
import: "import { Anchor } from '@klink-ui/core';"
source: 'mantine-core/src/Anchor/Anchor.tsx'
docs: 'core/Anchor.mdx'
---

import { AnchorDemos } from '@klink-ui/demos';

## Usage

Anchor is a wrapper around [Text](/core/text/) component with `component` prop set to `a` by default.
It supports the same props as Text component.

<Demo data={AnchorDemos.usage} />

## Polymorphic component

Anchor is a [polymorphic component](/guides/polymorphic/), you can change root element:

```tsx
import { Anchor } from '@klink-ui/core';

function Demo() {
  return (
    <Anchor component="button" type="button">
      Anchor as button
    </Anchor>
  );
}
```
