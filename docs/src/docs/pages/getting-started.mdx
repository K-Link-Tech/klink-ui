---
title: 'Getting started'
hideToc: true
---

import { NextSteps } from '../../components/NextSteps/NextSteps.tsx';
import { FrameworksControls } from '../../components/FrameworksControls/FrameworksControls.tsx';
import { PackagesInstallation } from '../../components/PackagesInstallation/PackagesInstallation.tsx';

# Welcome to Klink UI!

## Framework guides

<FrameworksControls />

## Get started without framework

<PackagesInstallation />

Wrap your application root component with [MantineProvider](/theming/k-link-provider/):

```tsx
import { MantineProvider } from '@k-link/core';

function Demo() {
  return (
    <MantineProvider withGlobalStyles withNormalizeCSS>
      <App />
    </MantineProvider>
  );
}
```

All set! You can now import and use Klink UI components anywhere in your application:

```tsx
import { Button } from '@k-link/core';

function Demo() {
  return <Button>Click me!</Button>;
}
```

## Start learning

<NextSteps />
